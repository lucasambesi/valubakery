@page "/"

<PageTitle>Index</PageTitle>

<MudText Typo="Typo.h4" Color="Color.Primary">Welcome to ValuBakery 🎂</MudText>

<MudButton Variant="Variant.Filled" Color="Color.Secondary" OnClick="@(() => clicked = !clicked)">
    @(clicked ? "Clicked!" : "Click me")
</MudButton>

@code {
    private bool clicked = false;
}
